# .talos/project.env — identidad del proyecto (fuente runtime de las CLIs Go).
# NO-secreto: committear. Mirror de openspec/config.yaml:jira — mantener en sync.
# Los secrets (JIRA_EMAIL, JIRA_API_TOKEN) van en .env (gitignoreado), NUNCA acá.
JIRA_SITE_URL=https://tablex.atlassian.net
JIRA_PROJECT_KEY=TAL
JIRA_PROJECT_ID=10099
