# Secrets only — NUNCA commitear.
# Identidad no-secreta (JIRA_SITE_URL, JIRA_PROJECT_KEY, JIRA_PROJECT_ID) vive
# en .talos/project.env (committeable) en la raíz del repo.
JIRA_EMAIL=
JIRA_API_TOKEN=
